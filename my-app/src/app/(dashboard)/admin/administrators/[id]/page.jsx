"use client";

import TeacherProfilePage from "../../teachers/[id]/page";

// Re-use the same component as the logic for "from=administrators" is already inside it.
export default TeacherProfilePage;
